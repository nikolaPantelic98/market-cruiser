package com.marketcruiser.common.entity;

/**
 * The AuthenticationType enum represents the two authentication types that are available in the system: DATABASE and GOOGLE.
 * It is used to specify the type of authentication to be used when a user logs in to the system.
 */
public enum AuthenticationType {

    DATABASE, GOOGLE
}
